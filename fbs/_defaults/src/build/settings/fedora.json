{
    "installer": "${app_name}.rpm",
    "gpg_preset_passphrase": "/usr/libexec/gpg-preset-passphrase",
    "repo_url": "https://fbs.sh/${fbs_user}/${app_name}/${repo_subdir}",
    "gpgkey_url": "https://fbs.sh/${fbs_user}/${app_name}/public-key.gpg",
    "repo_subdir": "rpm"
}